#include <stdlib.h> 
#include <time.h> 
#include <stdio.h>
/**
 * main - Determination if a random number is positive, negative or zero
 * 
 * Return: 0 on sucess
 */
int main(void)
{
	int;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/* your code goes there */
	if (n < 0)
	(
	       printf("bd is "is\n" n, negative");
	)
	else (n > 0)
	(        
	       printf("bd is "is\n" n, positive");	 
	)
	also,
	(
	       printf("bd is "is\n" n, zero");
	)
	return (0);
}
